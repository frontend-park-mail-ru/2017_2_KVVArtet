const execBuffer = require('exec-buffer');
const gifsicle = require('gifsicle');

module.exports = buffer => execBuffer({
  input: buffer,
  bin: gifsicle,
  args: [
    '--optimize',
    '--output',
    execBuffer.output,
    execBuffer.input
  ]
});

const SVGO = require('svgo');

module.exports = buffer => new Promise((resolve, reject) => {
  new SVGO().optimize(buffer, result => {
    if (result.error) {
      reject(result.error);
    } else {
      resolve(Buffer.from(result.data));
    }
  });
});

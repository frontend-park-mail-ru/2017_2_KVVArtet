const execa = require('execa');
const jpegoptim = require('jpegoptim-bin');

module.exports = buffer => execa.stdout(jpegoptim, [
  '--strip-all',
  '--strip-iptc',
  '--strip-icc',
  '--stdin',
  '--stdout'
], {
  encoding: null,
  input: buffer
});

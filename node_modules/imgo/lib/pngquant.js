const execa = require('execa');
const pngquant = require('pngquant-bin');

module.exports = buffer => execa.stdout(pngquant, [
  '--speed=1',
  '--force',
  '256'
], {
  encoding: null,
  input: buffer
});
